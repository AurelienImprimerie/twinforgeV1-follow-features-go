# Stripe Secret Keys for Product Creation Scripts
# Copy these lines to your main .env file at the project root

# Test Mode (Development)
STRIPE_TEST_SECRET_KEY=sk_test_51MjlukKVCSnP5L8O...

# Live Mode (Production) - Only add when ready for production
STRIPE_LIVE_SECRET_KEY=sk_live_51MjlukKVCSnP5L8O...

# Where to get these keys:
# 1. Go to https://dashboard.stripe.com
# 2. Click "Developers" in the left menu
# 3. Click "API keys"
# 4. Copy your Secret key (starts with sk_test_ or sk_live_)

# Security reminder:
# - NEVER commit these keys to git
# - Keep test and live keys separate
# - Rotate keys if compromised
